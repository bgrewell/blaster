> ⚠️ **Archived — superseded by [loom](https://github.com/bgrewell/loom).** Part of the consolidation of old traffic-generation projects into loom. The useful ideas here were captured as a blueprint in loom (`docs/blueprints/`). Kept read-only for reference.

# Blaster
Blaster is a simple tcp/udp traffic generation library designed to enable simple yet flexible synthetic traffic generation.
